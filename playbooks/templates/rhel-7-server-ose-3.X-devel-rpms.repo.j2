[rhel-7-server-ose-{{ release_version }}-{{ build_version }}-devel-rpms]
name = A repository of dependencies for Atomic OpenShift {{ release_version }}
baseurl = https://mirror2.openshift.com/enterprise/all/{{ release_version }}/{{ build_version }}/x86_64/os/
failovermethod = priority
gpgcheck = 0
username = {{ mirror_username }}
password = {{ mirror_password }}
enabled = 1
